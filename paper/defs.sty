%% Package loading and macro definitions for usage when writing papers

\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{defs}[%
  2020/04/16 v0.1 Packages and definitions for paper writing]

\RequirePackage[UKenglish]{babel}

\RequirePackage{graphicx}

\RequirePackage{csquotes}

\RequirePackage[%
  group-minimum-digits=4,%
  list-final-separator={, and },%
  add-integer-zero=false,%
  free-standing-units,%
  round-mode=figures,%
  round-precision=3,%
  binary-units,%
  detect-weight=true,%
  detect-inline-weight=math,%
]{siunitx}

\RequirePackage[%
  ruled,%
  noline,%
  noend,%
  linesnumbered,%
]{algorithm2e}

%\RequirePackage[amsmath]{ntheorem}
%\theoremseparator{:}
\newtheorem{resq}{Research Question}
\newtheorem{hyp}{Hypothesis}

%\RequirePackage[newfloat=true]{minted}
%\setminted{%
%  autogobble,%
%  breaklines=true,%
%  fontsize=\footnotesize,%
%  linenos=false,%
%  resetmargins=true,%
%  xleftmargin=1em,%
%  xrightmargin=1em,%
%  frame=single,%
%}
\RequirePackage{listings}
\lstset{%
  frame=single,%
  extendedchars=true,
  basicstyle=\footnotesize\ttfamily,
  keywordstyle=\color{blue}\bfseries,
  showstringspaces=false,
  showspaces=false,
  tabsize=2,
  breaklines=true,
  showtabs=false,
  captionpos=b
}
\lstloadlanguages{Java,Python}
\newcommand{\mintinline}[2]{\texttt{#2}}
\newcommand{\sourcecode}[1]{\texttt{#1}}

\RequirePackage{booktabs}

\RequirePackage{microtype}
\clubpenalty=10000
\widowpenalty=10000
\displaywidowpenalty=10000
% See https://tex.stackexchange.com/a/109188/14622 for the following:
\SetExtraKerning{% Manually adjust kerning settings
  encoding = {OT1,T1,T2A,LY1,OT4,QX,T5,TS1,EU1,EU2}% all text
}{%
  \textemdash = {167,167},% add a thinspace around the m-dash
  — = {167,167}% add a thinspace around the m-dash
}

\RequirePackage{pifont}

\RequirePackage{todonotes}
\newcommand{\sltodo}[1]{%
  \todo[color=cyan,size=tiny]{SL\@: #1}%
}
\newcommand{\gftodo}[1]{%
  \todo[color=lime,size=tiny]{GF\@: #1}%
}

\RequirePackage{tikz}
\newcommand*\circled[1]{%
  \tikz[baseline=(char.base)]{%
    \node[%
      shape=circle,%
      draw,%
      inner sep=1pt,%
      fill=black!20,%
    ] (char) {\small #1};%
  }%
}

\RequirePackage{xspace}

\RequirePackage{relsize}

\RequirePackage[capitalise]{cleveref}

%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%%

\newcommand{\toolname}[1]{%
  \textsc{#1}\xspace%
}

\newcommand{\pynguin}{%
  \toolname{Pynguin}%
}
\newcommand{\Pynguin}{%
  \pynguin%
}
\newcommand{\evosuite}{%
  \toolname{EvoSuite}%
}
\newcommand{\EvoSuite}{%
  \evosuite%
}
\newcommand{\mutmut}{%
  \toolname{MutMut}%
}
\newcommand{\MutMut}{%
  \mutmut%
}
\newcommand{\mutpy}{%
  \toolname{MutPy}%
}
\newcommand{\MutPy}{%
  \mutpy%
}

\newcommand{\command}[1]{%
  \texttt{#1}\xspace%
}

\newcommand{\summary}[2]{%
  \vspace{-0.2cm}%
  \begin{center}%
    \colorbox{gray!20}{%
      \parbox{\linewidth}{%
        \textbf{\textsf{Summary (\textit{#1})}:}~%
        #2%
      }%
    }%
  \end{center}%
}

\newcommand\mycheckmark{\ding{51}}
\newcommand\mycrossmark{\ding{55}}

\newcommand\effectsize{\ensuremath{\hat{A}_{12}}}
\newcommand\pvalue{\ensuremath{p\mathrm{-value}}}

\DeclareMathOperator{\abs}{abs}
\DeclareMathOperator{\cov}{cov}
\DeclareMathOperator{\lev}{lev}

\newcommand\CC{%
  C\nolinebreak[4]\hspace{-.05em}\raisebox{.4ex}{\relsize{-3}{\textbf{++}}}%
}
